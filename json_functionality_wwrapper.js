const fs = require("fs");

class JSON_FUNCTIONS {
  append_to_json_arr() {}

  delete_from_json_arr() {}
}

const net = require("net");
const { Client } = require("client-package");

Client.connect();
